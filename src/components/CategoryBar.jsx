import React from 'react';

const CategoryBar = () => {
  return (
    <div className="hidden">
      {/* CategoryBar is hidden in this expanded home mode as categories are inside Navbar */}
    </div>
  );
};

export default CategoryBar;